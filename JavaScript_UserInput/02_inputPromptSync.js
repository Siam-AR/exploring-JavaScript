// Before running this code, install prompt-sync using:
// npm install prompt-sync

const prompt = require("prompt-sync")(); 

const name = prompt("Enter your name: "); 

console.log(`Hello, ${name}!`);
